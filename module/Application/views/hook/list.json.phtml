"responseData":{"count": "<?php echo count($data['hooks']); ?>", "hooks": [
<?php 
	$hooksInfo = array();
	foreach ($data['hooks'] as $hook) {
		$hooksInfo[] = $hook->toJson();
	}
	echo implode(',', $hooksInfo);
?>
]}